{
  "name": "address-book-demo",
  "version": "1.0.0",
  "scripts": {
    "api-start": "cd api && dotnet run --project AddressBook.Api",
    "api-test": "cd api && dotnet test",
    "api-watch": "cd api && dotnet watch --project AddressBook.Api run",
    "client-build": "cd client && npm build",
    "client-e2e": "cd client && npm run e2e",
    "client-lint": "cd client && npm run lint",
    "client-ng": "cd client && npm run ng",
    "client-start": "cd client && npm start",
    "client-test": "cd client && npm test",
    "db-start": "cd db && docker-compose up -d",
    "db-stop": "cd db && docker-compose down -v"
  }
}
